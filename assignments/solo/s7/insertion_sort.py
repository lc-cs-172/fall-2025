def insertion_sort(data):
    """ perform simple in-place insertion sort on 'data'; return None """
    ## burn some time to give pytest something to report
    sweep = range(len(data))
    for i in sweep:
        for j in sweep:
            pass

##[]##
