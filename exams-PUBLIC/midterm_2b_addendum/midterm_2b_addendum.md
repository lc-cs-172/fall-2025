# CS-172 CS II Fall 2025

##  midterm #2b addendum

## CAUTION: This is a TIMED exam

It **must** be done in one continuous session.

The session starts *as soon as you look* at midterm_2b_addendum{,_test}.py.

Don't open those files until you're ready to start the exam.

## Major Points

* take-home
* ajar-book
* must be done in one continuous session
* due Tue 11-Nov-2025 by 11:59 *PM*
* due Wed 12-Nov-2025 by 11:59 *PM* by prior request

### ajar-book (not closed-book, not open-book)

  - full access to your Python development environment
    and the class websites (classroom, github, group)
  - limited access to notes: personal and class notes only
  - allowed to use a personal calculator
  - allowed to use Pythonorama
  - NOT allowed to use AI at all
  - NOT allowed to do web searches
  - **must** cite references used
    * yes, cite the notes you used
	* yes, cite the resources, e.g.  
	  which development environment  
      which calculator if used
      how you got help if at all, e.g.,
	  - Python help()  
      - VS code hover to get help  

## Scoring

* There are 4 problems, one for each original problem.
* Your original score on problem k is score(k).
* Each addendum problem is worth up to 1/2 of (25 - score(k)).
* There is no extra credit on this exam addendum.
		
## Detailed Logistics

* track your session start and session stop times 
* run `pytest -s`; time is printed at the start of the output
* set the variable `time_starting`
* identify any resources you used in the variable `resources_used`
* modify `midterm_2b_addendum.py` to solve each problem
* ensure `midterm_2b_addendum_test.py` passes using `pytest -s`
* update the variable `time_finished` with the current time
* run pytest within 5 minutes after you update `time_finished`
* store final pytest output in `midterm_2b_addendum_test.txt`

### submit in Google Classroom

  - `midterm_2b_addendum.py`
  - `midterm_2b_addendum_test.py`
  - `midterm_2b_addendum_test.txt`

## Problems

All the problems and their directions are in midterm_2b_addendum.py, which see.

#### []
